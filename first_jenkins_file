pipeline {
  agent {
    docker {image 'node:16-alpine'}
  }
  stages {
    stage('test01') {
      steps {
        sh 'mkdir newfolder'
        Sh 'cd newfolder'
        sh 'touch newfile.txt'
      }
    }
  }
}

